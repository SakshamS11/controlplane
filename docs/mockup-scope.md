# Preview Scope

This is a mockup-only dashboard preview.

It intentionally uses:

- mock data
- local UI state
- no backend
- no database
- no external APIs

For real privacy, keep the repository private and enable Vercel Deployment Protection.
